Test for "shift" script command.

This command shifts vertically the portion of a curve delimited by the markers.

Open the file "shift.plt"

Use the data cursor (left mouse click) and two markers (Ctrl+M) to select two points at about X=2 and X=4.

Enter "shift Y -1" in script window and execute.

You should obtain something like the shift.png image.
