Test for "despike" script command.

This command removes spikes in a vector. It takes two parameters, 
the vector name and the window size (an odd integer) in number of points.

Open the file "despike.plt"

Enter "despike I 3" in the script window and execute (Ctrl+E)

The spikes have been removed.
