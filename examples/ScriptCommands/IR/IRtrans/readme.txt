Test of "IRtrans" script command.

Open the file "IRtrans.txt"

The intensities are in absorbance.

To convert them in transmittance, type "IRtrans" in the Script window and execute (Ctrl+E).

You should get the spectrum in transmittance (like in IRtrans.png).

