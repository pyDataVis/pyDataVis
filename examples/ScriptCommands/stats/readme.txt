Test for the "stats" script command.

The stats command provides statistics on a vector.

Open the file "stats.plt".

Type "stats Y" in the Script window and execute (Ctrl+E).

As shown in stats.png, this open a window giving the statistics on Y.

