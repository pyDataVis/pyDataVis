Test for the script command "fft"

Open the file "fft.plt"

Type "fft Y" in the Script window and then execute (Ctrl+E).

This compute the FFT of Y and create a new file named "Y-fft.txt".
This file is loaded in a new pyDataVis window (see Y-fft.png).
