Test for the script command "mergeb"

This command merges two data blocks.

Open the file "mergeb.txt" which contains two data blocks with only one vector.

Type "mergeb 1 2" in the Script window and execute (Ctrl+E).

The Information window indicates that now there is only one data block containing two vectors.

To see the result, use the option "Update from data" in the File menu.
