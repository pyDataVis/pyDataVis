Test the option "Append" in the File menu.

The "Append" option is used to append data from another file at the end of the current data.
Both file must contain the same number of vectors.

Open the file "append-1.plt"

Use the option "Append" to select the file "append-2.plt"

The plotting area shows the union of both files (append.png).
